@model Domain.Models.DocumentFlow

@{
    ViewData["Title"] = "Заява на відпустку";
    ViewData["DocumentTitle"] = "ЗАЯВА";
    ViewData["DocumentSubtitle"] = "Про надання відпустки";
    Layout = null;
}

<style>
    body {
        font-family: 'Times New Roman', Times, serif;
        font-size: 14pt;
        line-height: 1.5;
        margin: 20mm 10mm 20mm 30mm;
    }

    .header {
        text-align: center;
        margin-bottom: 20mm;
    }

    .content {
        text-align: justify;
    }

    .footer {
        margin-top: 20mm;
    }

    .signature {
        display: flex;
        justify-content: space-between;
        margin-top: 10mm;
    }
</style>

@await Html.PartialAsync("_DocumentHeader", Model)

<div class="content">
    <p>Прошу надати відпустку @Model.ServicemanFullName.</p>
    <p>Зміст документа:<br>@Model.Content</p>
    <p>Тип документа: @Model.DocumentType</p>
    <p>Статус: @Model.Status</p>
</div>

<div class="footer">
    <div class="signature">
        <div>
            <p>Заявник</p>
            <p>_____________________ @Model.ServicemanFullName</p>
        </div>
        <div>
            <p>Дата: @(Model.CreatedDate?.ToString("dd.MM.yyyy"))</p>
        </div>
    </div>
</div>