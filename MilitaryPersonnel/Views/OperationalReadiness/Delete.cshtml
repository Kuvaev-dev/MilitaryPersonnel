@model Domain.Models.OperationalReadiness

@{
    ViewData["Title"] = "Delete Operational Readiness";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(model => model.Id)

    <div class="form-horizontal">
        <div class="col-md-10">
            <h2>Are you sure you want to delete this operational readiness record?</h2>

            <dl class="dl-horizontal">
                <div class="form-group">
                    <dt class="col-sm-2">Serviceman</dt>
                    <dd class="form-control-static">@Model.ServicemenFullName</dd>
                </div>
                <div class="form-group">
                    <dt class="col-sm-2">Readiness Status</dt>
                    <dd class="form-control-static">@Model.ReadinessStatus</dd>
                </div>
                <div class="form-group">
                    <dt class="col-sm-2">Assessment Date</dt>
                    <dd class="form-control-static">@Model.AssessmentDate</dd>
                </div>
            </dl>
            <div class="form-group">
                <input type="submit" value="Delete" class="btn btn-danger" />
                @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-default" })
            </div>
        </div>
    </div>
}